// Converte numero Hexadecimal para Decimal
#include <stdio.h>

int main(void) {
    int n = 0;

    printf("Digite o numero Hexadecimal: ");    // Entrada de Dados
    scanf("%x", &n);

    printf("Numero em decimal: %d", n);         // Saida de dados.
    return 0;
}